{
  "name": "NegativeIcon",
  "version": "0.1.0",
  "description": "An app for making visual comparisons.",
  "main": "lib/main.js",
  "repository": "https://github.com/atdrago/negative-icon.git",
  "license": "MIT",
  "scripts": {
    "start": "export NODE_ENV=development && export ELECTRON_ENABLE_LOGGING=true && electron .",
    "icon": "iconutil -c icns negative.iconset"
  },
  "author": "Adam Drago",
  "dependencies": {
    "electron-window": "^0.8.0"
  },
  "devDependencies": {
    "electron-prebuilt": "^1.0.2",
    "electron-rebuild": "^1.1.3"
  },
  "engines": {
    "node": ">=4.x <=5.x"
  },
  "os": [ "darwin" ]
}
